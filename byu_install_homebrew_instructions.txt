1. CD into 'USERS/canyons' (should see Desktop, Documents, Downloads, etc.)
2. Run 'mkdir -p ~/homebrew && curl -L https://github.com/Homebrew/brew/tarball/master | tar xz --strip 1 -C homebrew'
3. Run 'export PATH=$PATH:/Users/canyons/homebrew/bin/'